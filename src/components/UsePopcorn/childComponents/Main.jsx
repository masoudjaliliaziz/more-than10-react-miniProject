import React, { useState } from "react";
import ListBox from "./ListBox";
import WathedBox from "./WathedBox";

function Main({ children }) {
  return <main className="main">{children}</main>;
}

export default Main;
